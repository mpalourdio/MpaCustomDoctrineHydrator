language: php

php:
  - 5.5
  - 5.6
  - hhvm

matrix:
  allow_failures:
    - php: hhvm

before_script:
  - composer self-update
  - composer update --prefer-source

script:
  - ./vendor/bin/phpcs --standard=PSR2 ./src/ ./tests/